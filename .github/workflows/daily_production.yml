name: Video Production
on:
  workflow_dispatch:
    inputs:
      script_id:
        description: 'Script ID (0, 1, 2)'
        required: true
        type: choice
        options:
        - 0
        - 1  
        - 2
      script_data:
        description: 'Script data JSON'
        required: true
        type: string

jobs:
  produce-video:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: Parse script data
        id: script
        run: |
          echo "SCRIPT_DATA=${{ github.event.inputs.script_data }}" >> $GITHUB_OUTPUT
          
      - name: Produce video
        run: |
          echo "🎬 Production vidéo pour script ${{ github.event.inputs.script_id }}"
          echo "📄 Données: ${{ steps.script.outputs.SCRIPT_DATA }}"
          # Ici: ton code de production vidéo existant
