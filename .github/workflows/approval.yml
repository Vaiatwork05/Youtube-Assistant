name: Script Approval
on: 
  repository_dispatch:
    types: [script_approved]

jobs:
  produce-video:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Produce Video
        run: |
          echo "Production vidéo lancée pour script ${{ github.event.client_payload.script_id }}"
          # Ton code de production ici
